import { TolocaldatePipe } from './tolocaldate.pipe';

describe('TolocaldatePipe', () => {
  it('create an instance', () => {
    const pipe = new TolocaldatePipe();
    expect(pipe).toBeTruthy();
  });
});
